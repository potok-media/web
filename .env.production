# Default backend (BFF) for production builds. Public URL, not a secret.
# Used as the default connection-profile gateway across all containers
# (browser, Android WebView app, Apple TV / third-party launchers).
VITE_DEFAULT_BFF_URL=https://api.potok.rip
